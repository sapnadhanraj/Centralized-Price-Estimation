<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>iPhone Price Comparison - <%= selectedProduct.name %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 20px 0;
        }

        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .product-header {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .product-info {
            display: flex;
            align-items: center;
            gap: 30px;
        }

        .product-image {
            width: 150px;
            height: 150px;
            object-fit: contain;
            background: #f8f9fa;
            border-radius: 10px;
            padding: 10px;
        }

        .product-details h1 {
            color: #333;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .product-specs {
            display: flex;
            gap: 25px;
            flex-wrap: wrap;
        }

        .spec-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #666;
            font-size: 15px;
        }

        .spec-item i {
            color: #667eea;
            font-size: 18px;
        }

        .comparison-section {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-title i {
            color: #667eea;
        }

        .loading-spinner {
            text-align: center;
            padding: 40px;
        }

        .spinner-border {
            width: 3rem;
            height: 3rem;
            color: #667eea;
        }

        .comparison-table {
            width: 100%;
            margin-top: 20px;
        }

        .comparison-table thead {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .comparison-table th {
            padding: 15px;
            font-weight: 600;
            text-align: left;
            border: none;
        }

        .comparison-table tbody tr {
            border-bottom: 1px solid #e9ecef;
            transition: all 0.3s;
        }

        .comparison-table tbody tr:hover {
            background: #f8f9fa;
            transform: scale(1.01);
        }

        .comparison-table td {
            padding: 15px;
            vertical-align: middle;
        }

        .price-cell {
            font-size: 20px;
            font-weight: 700;
            color: #28a745;
        }

        .best-price {
            background: #d4edda !important;
            border-left: 4px solid #28a745;
        }

        .source-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .badge-amazon {
            background: #ff9900;
            color: white;
        }

        .badge-flipkart {
            background: #2874f0;
            color: white;
        }

        .badge-apple {
            background: #000;
            color: white;
        }

        .badge-croma {
            background: #00a699;
            color: white;
        }

        .badge-reliance {
            background: #e42529;
            color: white;
        }

        .view-link {
            display: inline-block;
            padding: 8px 16px;
            background: #667eea;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            font-size: 13px;
            transition: all 0.3s;
        }

        .view-link:hover {
            background: #5568d3;
            color: white;
            text-decoration: none;
            transform: translateY(-2px);
        }

        .back-button {
            display: inline-block;
            padding: 12px 24px;
            background: white;
            color: #667eea;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            margin-bottom: 20px;
            transition: all 0.3s;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .back-button:hover {
            background: #f8f9fa;
            color: #5568d3;
            text-decoration: none;
            transform: translateY(-2px);
        }

        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .stat-value {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            opacity: 0.9;
        }

        .no-results {
            text-align: center;
            padding: 60px 20px;
            color: #666;
        }

        .no-results i {
            font-size: 64px;
            color: #ccc;
            margin-bottom: 20px;
        }

        .product-type {
            color: #666;
            font-size: 14px;
        }

        .maker-cell {
            font-weight: 600;
            color: #333;
        }

        .model-cell {
            color: #666;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <a href="/home" class="back-button">
            <i class="fas fa-arrow-left"></i> Back to Dashboard
        </a>

        <!-- Product Header -->
        <div class="product-header">
            <div class="product-info">
                <div>
                    <img src="https://store.storeimages.cdn-apple.com/4668/as-images.apple.com/is/iphone-15-pro-finish-select-202309-6-7inch-naturaltitanium?wid=5120&hei=2880&fmt=p-jpg&qlt=80&.v=1692845702992" 
                         alt="<%= selectedProduct.name %>" 
                         class="product-image"
                         onerror="this.src='https://via.placeholder.com/150?text=iPhone'">
                </div>
                <div class="product-details">
                    <h1><%= selectedProduct.name %></h1>
                    <div class="product-specs">
                        <div class="spec-item">
                            <i class="fas fa-memory"></i>
                            <span><%= selectedProduct.storage %></span>
                        </div>
                        <div class="spec-item">
                            <i class="fas fa-palette"></i>
                            <span><%= selectedProduct.color %></span>
                        </div>
                        <div class="spec-item">
                            <i class="fas fa-mobile-alt"></i>
                            <span><%= selectedProduct.size %></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Comparison Results -->
        <div class="comparison-section">
            <!-- Demo Data Notice -->
            <div class="alert alert-info" role="alert" style="border-left: 4px solid #667eea; background: #e7f3ff;">
                <i class="fas fa-info-circle"></i> <strong>Demo Mode:</strong> This comparison uses sample price data for demonstration purposes. 
                In production, this would fetch real-time data from a database with scheduled price updates from e-commerce websites.
            </div>

            <div class="section-title">
                <i class="fas fa-chart-line"></i>
                Price Comparison Results
            </div>

            <% if (products && products.length > 0) { %>
                <!-- Statistics Cards -->
                <div class="stats-cards">
                    <div class="stat-card">
                        <div class="stat-value"><%= products.length %></div>
                        <div class="stat-label">Results Found</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-value">
                            ₹<%= Math.min(...products.map(p => parseFloat(p.price.replace(/[₹,]/g, '')))).toLocaleString() %>
                        </div>
                        <div class="stat-label">Best Price</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-value">
                            ₹<%= Math.max(...products.map(p => parseFloat(p.price.replace(/[₹,]/g, '')))).toLocaleString() %>
                        </div>
                        <div class="stat-label">Highest Price</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-value">
                            ₹<%= (Math.max(...products.map(p => parseFloat(p.price.replace(/[₹,]/g, '')))) - 
                                 Math.min(...products.map(p => parseFloat(p.price.replace(/[₹,]/g, ''))))).toLocaleString() %>
                        </div>
                        <div class="stat-label">Price Difference</div>
                    </div>
                </div>

                <!-- Comparison Table -->
                <table class="table comparison-table">
                    <thead>
                        <tr>
                            <th>Product Name</th>
                            <th>Type</th>
                            <th>Maker</th>
                            <th>Model</th>
                            <th>Price</th>
                            <th>Source</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% 
                        // Sort products by price (lowest first)
                        const sortedProducts = products.sort((a, b) => {
                            const priceA = parseFloat(a.price.replace(/[₹,]/g, ''));
                            const priceB = parseFloat(b.price.replace(/[₹,]/g, ''));
                            return priceA - priceB;
                        });

                        const lowestPrice = parseFloat(sortedProducts[0].price.replace(/[₹,]/g, ''));
                        
                        sortedProducts.forEach((product, index) => { 
                            const currentPrice = parseFloat(product.price.replace(/[₹,]/g, ''));
                            const isBestPrice = currentPrice === lowestPrice;
                            
                            // Determine source badge class
                            let badgeClass = 'badge-amazon';
                            const sourceLower = product.source.toLowerCase();
                            if (sourceLower.includes('flipkart')) badgeClass = 'badge-flipkart';
                            else if (sourceLower.includes('apple')) badgeClass = 'badge-apple';
                            else if (sourceLower.includes('croma')) badgeClass = 'badge-croma';
                            else if (sourceLower.includes('reliance')) badgeClass = 'badge-reliance';
                        %>
                        <tr class="<%= isBestPrice ? 'best-price' : '' %>">
                            <td>
                                <strong><%= product.name %></strong>
                                <% if (isBestPrice) { %>
                                    <span class="badge badge-success ml-2">Best Deal</span>
                                <% } %>
                            </td>
                            <td class="product-type">Smartphone</td>
                            <td class="maker-cell">Apple</td>
                            <td class="model-cell"><%= selectedProduct.name.replace('iPhone ', '') %></td>
                            <td class="price-cell"><%= product.price %></td>
                            <td>
                                <span class="source-badge <%= badgeClass %>">
                                    <%= product.source %>
                                </span>
                            </td>
                            <td>
                                <a href="<%= product.link %>" target="_blank" class="view-link">
                                    <i class="fas fa-external-link-alt"></i> View
                                </a>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="no-results">
                    <i class="fas fa-search"></i>
                    <h3>No Results Found</h3>
                    <p>We couldn't find any prices for this product at the moment.</p>
                    <p class="text-muted">Try again in a few moments or search for a different product.</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
</body>
</html>
